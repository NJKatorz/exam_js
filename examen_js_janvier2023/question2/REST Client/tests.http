@baseUrl = http://localhost:3000

### Enregistrer l’achat d’un utilisateur
POST {{baseUrl}}/purchases
Content-Type: application/json

{
    "pseudo": "user1",
    "idProduct": 2,
    "quantity": 3
}

### Enregistrer l’achat d’un utilisateur
POST {{baseUrl}}/purchases
Content-Type: application/json

{
    "pseudo": "user1",
    "idProduct": 1,
    "quantity": 4
}


### Enregistrer l’achat d’un utilisateur
POST {{baseUrl}}/purchases
Content-Type: application/json

{
    "pseudo": "user2",
    "idProduct": 3,
    "quantity": 10
}

### Enregistrer l’achat d’un utilisateur
POST {{baseUrl}}/purchases
Content-Type: application/json

{
    "pseudo": "user3",
    "idProduct": 3,
    "quantity": 8
}

### Récupérer l’utilisateur achetant le plus fréquemment un produit
GET {{baseUrl}}/purchases/3

### Récupérer l’utilisateur achetant le plus fréquemment un produit
GET {{baseUrl}}/purchases/2

### Récupérer le produit recommandé pour un utilisateur
GET {{baseUrl}}/recommendations/user2

### Récupérer le produit recommandé pour un utilisateur
GET {{baseUrl}}/recommendations
